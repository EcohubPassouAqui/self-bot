# self-bot


```md
[ SELF BOT ]

+ .venv -3.9
    + py -3.9 -m venv .venv
+ python 3.9
    + python39.exe
+ config.json configurar token conta usuario
+ databases ( cria arquivo.txt nome servidor com cargos clonados )
+ requirements.txt

- import cgi removido
- import audioop removido
```